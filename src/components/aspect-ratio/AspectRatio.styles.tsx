import styled from "styled-components";

export const Outer = styled.div`
	position: relative;
`;

export const Inner = styled.div`
	width: 100%;
	height: 100%;
	top: 0;
	left: 0;
	position: absolute;
	overflow: hidden;
`;
